<!DOCTYPE html>
<!DOCTYPE html>
<html>
    <head>
        <title>Parcelmagic</title>
        
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black" />
        <meta name="format-detection" content="telephone=no"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        
        <link rel="apple-touch-startup-image" href="<%= static_path(@conn, "/lib/durandal/img/ios-startup-image-landscape.png") %>" media="(orientation:landscape)" />
        <link rel="apple-touch-startup-image" href="<%= static_path(@conn, "/lib/durandal/img/ios-startup-image-portrait.png") %>" media="(orientation:portrait)" />
        <link rel="apple-touch-icon" href="<%= static_path(@conn, "/lib/durandal/img/icon.png") %>"/>       
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        
        <link rel="stylesheet" href="<%= static_path(@conn, "/lib/materialize/css/materialize.min.css") %>" />
        <link rel="stylesheet" href="<%= static_path(@conn, "/lib/font-awesome/css/font-awesome.min.css") %>" />
        <link rel="stylesheet" href="<%= static_path(@conn, "/css/ie10mobile.css") %>" />
        <link rel="stylesheet" href="<%= static_path(@conn, "/lib/durandal/css/durandal.css") %>" />
        <link rel="stylesheet" href="<%= static_path(@conn, "/css/starterkit.css") %>" />
        <link rel="stylesheet" href="<%= static_path(@conn, "/lib/toastr/toastr.min.css") %>" />
        
        <script type="text/javascript">
            if (navigator.userAgent.match(/IEMobile\/10\.0/)) {
                var msViewportStyle = document.createElement("style");
                var mq = "@@-ms-viewport{width:auto!important}";
                msViewportStyle.appendChild(document.createTextNode(mq));
                document.getElementsByTagName("head")[0].appendChild(msViewportStyle);
            }
        </script>
    </head>
    <main>
      <body>

        <%= @inner %>

      <script src="<%= static_path(@conn, "/lib/require/require.js") %>" data-main="app/main"></script>
      </body>
    </main>
    <footer class="page-footer">
      <div class="footer-copyright">
        <div class="container">
        © 2015 Copyright Parcelmagic
        <span class="grey-text text-lighten-4 right">Made with <i class="material-icons" style="font-size:15px">favorite</i> in Pflugerville</span>
        </div>
      </div>
    </footer>
</html>

